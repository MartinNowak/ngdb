tests[]=
	hello1
	hello2
	hello3
	recursive
	segfault

.DEFAULT: $(tests)

CTest(name) =
	CProgram($(name), $(name))

CXXTest(name) =
	CXXProgram($(name), $(name))

DTest(name) =
	DProgram($(name), $(name))

CTest(hello1)
CXXTest(hello2)
DTest(hello3)
CTest(recursive)
CTest(segfault)
